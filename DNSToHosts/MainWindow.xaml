<Window x:Class="DNSToHosts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DNSToHosts"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="14*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBox Name="DomainList" Grid.Column="0" Grid.Row="0" TextWrapping="Wrap" AcceptsReturn="True" ></TextBox>
        <TextBox Name="HostsFileOutput" Grid.Column="1" Grid.Row="0" TextWrapping="Wrap" AcceptsReturn="True" ></TextBox>
        <Button Grid.Column="0" Grid.Row="1" Content="Generate Hosts File ->" Click="GenerateHostsFileButtonClicked" ></Button>
    </Grid>
</Window>
